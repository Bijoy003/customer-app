apiVersion: v1
kind: Service
metadata:
  name: customer-app
  labels:
    app: customer-app
spec:
  type: NodePort
  selector:
    app: customer-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
